# Security policy for the portfolio edition

This repository must contain only synthetic or explicitly sanitised evidence.

Do not commit:

- API keys, tokens, passwords or cookies
- SSH keys or SSH configuration containing private endpoints
- production IP addresses or hostnames
- `.env` files
- live databases or database exports
- personal data
- internal production paths or credentials

The portfolio edition is intentionally disconnected from production systems.
